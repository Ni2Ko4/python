b = 0
while True:
    numbers = input('Введи число или Q для выхода').split()
    for num in numbers
        if num == 'q':
            return b


    numbers_list_int = list(map(lambda b: int(b), ))
    a = 0
    while a != len(numbers_list_int):
        b = numbers_list_int[a] + b
        a += 1
    print(b)